---
tags: [data, database, schemas, reports, analytics, index]
description: Navigation index for team-data repo
---

# team-data Index

Quick-reference map for this repo. Use the generated slice
`index/claude-index-team-data.json` for precise file paths.

## High-Value Files

| File | When to Read |
|------|-------------|
| `schemas/users.sql` | User and session table definitions |
| `reports/q1-summary.md` | Q1 platform usage metrics |

## Structure

```
team-data/
  schemas/
    users.sql         <- users + sessions table definitions
  reports/
    q1-summary.md     <- Q1 usage summary
```

## Tags in This Repo

`data`, `database`, `schemas`, `sql`, `reports`, `analytics`
