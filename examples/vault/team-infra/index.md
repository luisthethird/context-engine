---
tags: [infra, infrastructure, terraform, kubernetes, index]
description: Navigation index for team-infra repo
---

# team-infra Index

Quick-reference map for this repo. Use the generated slice
`index/claude-index-team-infra.json` for precise file paths.

## High-Value Files

| File | When to Read |
|------|-------------|
| `terraform/main.tf` | GCP resource definitions, cluster config |
| `kubernetes/deployment.yaml` | API service deployment spec |

## Structure

```
team-infra/
  terraform/
    main.tf           <- GCP provider + cluster definition
  kubernetes/
    deployment.yaml   <- API deployment spec
```

## Tags in This Repo

`infra`, `infrastructure`, `terraform`, `iac`, `kubernetes`, `k8s`, `containers`, `config`
